# Creative Execution Layer

This layer converts Visual Spec into real creative assets using external engines.

Flow:

Visual Spec
  → Layout engine (Open Design)
  → Scene/Image engine (Generative Media Skills)
  → Review engine (DeepEval)
  → Final asset mapped back to Google Sheet
